# 사내 정보 기입 요청 — `confidence: draft` 7건

이 문서는 **제가 채울 수 없는 항목**의 목록이다. 사내 환경에만 존재하는 정보라
추측해서 적으면 그럴듯한 오답이 되고, 오답이 RAG에 들어가면 사람이 그대로 따른다.
그래서 빈칸으로 두고 여기에 명시한다.

기입이 끝난 항목은 `kb/authored/<파일>.md`를 고치고 `confidence`를 `draft` → `verified`로
올린 뒤 재빌드한다.

```bash
python3 tools/build_v3.py --v1 <v1> --v23-chunks <v23> --v23-manifest <m> \
  --authored kb/authored --out kb
./tests/run_checks.sh
```

---

## 1. `v3-onboarding-day1` — 신규 사용자 첫날 경로

**담당 후보**: 플랫폼팀 / 개발생산성팀

| 항목 | 현재 상태 | 필요한 것 |
|---|---|---|
| 패키지 이름 | `corp-mcp-<name>` 플레이스홀더 | 실제 사내 패키지 네임스페이스 |
| 사내 마켓플레이스 주소 | 비어 있음 | `extraKnownMarketplaces`에 넣을 GitHub repo URL |
| 토큰 발급 경로 | "사내 포털" | 실제 포털 URL + 발급 절차 |
| `managed-settings.json` 배포 여부 | 미확인 | IT가 이미 배포 중인지, 아니면 이번에 도입하는지 |
| 온보딩 성공 지표 목표치 | 추정값(30분/1건/1일) | 실측 후 조정 |

**주의**: `curl -sSL https://내부호스트/install.sh | sh` 예시가 본문에 있다. 이 패턴은
문서가 권장하는 PreToolUse guard가 차단하는 대상이다. **사내 install 경로를 allowlist에
등록할지, uvx 경로만 쓸지 먼저 정하고** 그에 맞춰 본문을 정리해야 한다.

---

## 2. `v3-incident-runbook` — 장애 대응 런북

**담당 후보**: SRE / 운영팀

| 항목 | 현재 상태 | 필요한 것 |
|---|---|---|
| 온콜 연락 경로 | 비어 있음 | 심각도별 호출 대상, 에스컬레이션 순서 |
| S1 정지 권한자 | 비어 있음 | 누가 feature flag를 끌 수 있는가 |
| 심각도 목표 대응 시간 | 일반값(즉시/1시간/당일) | 사내 SLA와 정합 확인 |
| feature flag 시스템 | 비어 있음 | 실제 사용 중인 도구명과 토글 경로 |
| remote config 경로 | 비어 있음 | 프롬프트·모델 버전을 원격 전환하는 실제 수단 |

**가장 중요한 빈칸**: "누가 멈출 권한을 갖는가". 런북의 1단계가 "멈춘다"인데
권한자가 정해져 있지 않으면 그 단계에서 사고가 늘어난다.

---

## 3. `v3-budget-management` — 비용 예산 책정

**담당 후보**: 운영팀 / 재무 협의

| 항목 | 현재 상태 | 필요한 것 |
|---|---|---|
| 1인당 기준선 | 미측정 | **`ccusage`로 2주 실측 후** p90 산출 |
| 조직 월 예산 | 비어 있음 | 재무 배정액 |
| 알림 임계 4종 | **출발점 제안** | 실측 기준선 대비 재계산 |
| 서브에이전트 팬아웃 상한 | 비어 있음 | 실제 워크로드 기준 상한값 |
| 하드 한도 집행 수단 | 비어 있음 | 초과 시 실제로 차단할 방법(조직 정책? 게이트웨이?) |

**순서 주의**: 이 청크의 임계값은 전부 "기준선 대비 배수"로 쓰여 있다.
**기준선 실측이 선행되지 않으면 나머지 숫자는 의미가 없다.** 2주 측정이 먼저다.

---

## 4. `v3-compliance-audit` — 감사 대응

**담당 후보**: 법무 / 보안

| 항목 | 현재 상태 | 필요한 것 |
|---|---|---|
| EU AI Act 적용 여부 | **판단 보류** | 법무 확인 — 사내 개발 도구가 대상인지 |
| ISO/IEC 42001 인증 추진 여부 | 비어 있음 | 인증을 받을 것인지, 참조만 할 것인지 |
| 로그 보존 기간 | 비어 있음 | 사내 규정상 보존 기간 |
| 감사로그 저장소 | 비어 있음 | append-only 저장소가 실제로 있는가 |
| 감사 대응 책임자 | 비어 있음 | 7일 시나리오의 각 단계 담당 |

**법적 판단은 넣지 않았다.** 이 청크는 "무엇을 준비해야 하는가"만 다루고
"우리가 규제 대상인가"는 판단하지 않는다. 그 판단을 RAG 문서가 대신하면 안 된다.

---

## 5. `v3-rag-pipeline-design` — RAG 파이프라인

**담당 후보**: 이 저장소 담당자

| 항목 | 현재 상태 | 필요한 것 |
|---|---|---|
| Recall/MRR 목표치 | **출발점 제안** | 실측 후 달성 가능선으로 조정 |
| 벡터 DB 선택 | 미정 | 5종 후보 중 확정 |
| 리랭커 모델 | `bge-reranker` 계열 제안 | 실제 채택 모델 |
| 하이브리드 α 값 | 0.5~0.7 제안 | 골든셋으로 튜닝한 값 |

이 항목들은 **4단계(검색 성능 측정) 후 자동으로 채워진다.** 지금 억지로 정하지 않는다.

---

## 6. `v3-korean-rag` — 한국어 RAG

**담당 후보**: 이 저장소 담당자

| 항목 | 현재 상태 | 필요한 것 |
|---|---|---|
| 임베딩 모델 등급표 | **원본 v2.3 표기 인용** (자체 측정 아님) | 골든셋으로 Recall 직접 비교 |
| 형태소 분석기 도입 여부 | 3개 선택지 제시 | 하나 확정 |

모델 비교는 30분이면 끝난다. **"우수/무난" 등급을 그대로 신뢰하지 말 것** —
이 부분은 원본 문서의 표기를 옮긴 것이지 검증된 값이 아니다.

---

## 7. `v3-mcp-version-migration` — MCP 마이그레이션

**담당 후보**: 개발팀

이 청크는 **2026-08-05에 사실 정정을 거쳤다**(정정 C-001). 원본이 2026-07-28을 RC로
기술했으나 정식 stable로 확인돼 권고를 다시 썼다.

| 항목 | 현재 상태 | 필요한 것 |
|---|---|---|
| 사내 MCP 서버 현재 사양 버전 | 비어 있음 | 인벤토리 — 어느 서버가 어느 리비전인가 |
| SSE 사용 서버 존재 여부 | 비어 있음 | 있으면 최우선 이행 대상 |
| 스테이징 환경 유무 | 비어 있음 | 인증 강화는 롤백이 어려워 스테이징 선행 필요 |

---

## 기입 우선순위

| 순위 | 항목 | 이유 |
|---|---|---|
| 1 | 3번 비용 기준선 실측 | 2주가 걸리므로 가장 먼저 시작해야 끝난다 |
| 2 | 2번 정지 권한자 | 빈칸인 채로 사고가 나면 대응이 멈춘다 |
| 3 | 1번 마켓플레이스·토큰 경로 | 온보딩이 막힌다 |
| 4 | 7번 MCP 인벤토리 | 이행 계획의 전제 |
| 5 | 4번 법무 확인 | 리드타임이 길다. 일찍 걸어둘 것 |
| 6 | 5·6번 | 4단계 측정 후 자동 확정 |
