# 시험결과 (Test Results) — v3.0 지식 베이스

| 항목 | 내용 |
|---|---|
| 시험 일자 | 2026-08-05 |
| 시험 계획 | [`docs/TEST-PLAN.md`](TEST-PLAN.md) |
| 대상 | v1 (47청크) / v2.3 (135청크) / **v3.0 (100청크)** |
| 실행 명령 | `python3 tools/kb_audit.py <dir>`, `./tests/run_checks.sh` |
| 최종 판정 | **적재 가능** (품질 게이트 7/7 통과) |

---

## 1. 요약

| 시험 | v1 | v2.3 | **v3.0** |
|---|---|---|---|
| T-01 청크 크기 | WARN | WARN | WARN |
| T-02 자율성 | PASS | PASS | PASS |
| T-03 메타데이터 | PASS | **FAIL** | **PASS** |
| T-04 retrieval_questions | PASS | **FAIL** | **PASS** |
| T-05 참조 무결성 | PASS | PASS | PASS |
| T-06 중복 | PASS | **WARN** | **PASS** |
| T-07 카테고리 균형 | PASS | **WARN** | **PASS** |
| T-08 포맷 | PASS | PASS | PASS |
| T-13 에셋 무결성 | — | — | **PASS** (선언 0건, 픽스처로 작동 증명) |

v3.0은 FAIL 0건. 남은 WARN은 T-01 하나이며 사유는 §3에 기록했다.

---

## 2. 상세 수치

### T-01 청크 크기 (목표 800~1500 토큰, 추정치 ±20%)

| 세트 | n | 중앙값 | 평균 | 미달 | 초과 | 범위 내 |
|---|---|---|---|---|---|---|
| v1 | 47 | 658 | 691.7 | 32 | 0 | 31.9% |
| v2.3 | 135 | **298** | 392.4 | **122** | 1 | **8.9%** |
| **v3.0** | 100 | **876** | 949.9 | 39 | 6 | **55.0%** |

v2.3의 중앙값 298토큰은 심각한 과분할이다. 원인은 문서 1개를 definition/setup/operations/
exceptions/security/related/sources **7개 섹션으로 기계적으로 쪼갠 고정 템플릿**이다.
v3.0에서 같은 원본 문서의 조각을 목표 크기까지 재병합해 중앙값을 2.9배로 올렸다.

### T-03 메타데이터 완전성

| 세트 | 필수 누락 | 권장 누락 |
|---|---|---|
| v1 | 0 | 0 |
| v2.3 | **135** (`retrieval_questions` 전건) | **675** |
| v3.0 | 0 | 0 |

v2.3은 청크 파일 frontmatter에 `chunk_id`/`title`/`section`/`category`/`tags`만 두고
나머지를 `chunk-manifest.jsonl`로 분리했다. 그런데 그 매니페스트에도
`retrieval_questions`·`priority`·`confidence`·`freshness`·`audience`는 **애초에 존재하지 않는다.**
분리 보관이 아니라 부재였다.

### T-04 retrieval_questions

| 세트 | 청크당 평균 | 없는 청크 |
|---|---|---|
| v1 | 3.40 | 0 |
| v2.3 | **0.00** | **135** |
| v3.0 | **4.08** | 0 |

v3.0에서는 v1 청크의 사람 작성 질문을 그대로 승계하고, v2.3 유래 청크에는 섹션 종류별
템플릿으로 생성했다. 템플릿에 없는 카탈로그성 섹션은 실제 소제목에서 질문을 만들었다.
**자동 생성분은 `confidence: auto-merged`로 표시**해 사람 검토 전임을 드러냈다.

### T-05 참조 무결성

| 세트 | 깨진 참조 | 자기참조 | 고립 청크 |
|---|---|---|---|
| v1 | 0 | 0 | 0 |
| v2.3 | 0 | 0 | **135** |
| v3.0 | 0 | 0 | 0 |

v2.3 청크 파일에는 `related_chunks`가 없다(매니페스트에만 존재). 매니페스트 쪽 값도
"같은 원본 문서의 형제 조각 전부"라서 의미 기반 연결이 아니라 기계적 나열이다.
v3.0은 원본 형제 관계 + 태그 교집합 기반 보강을 함께 썼다.

### T-06 중복 / T-07 카테고리 균형

| 세트 | 중복쌍(Jaccard ≥0.45) | 카테고리 수 | 최대 점유율 |
|---|---|---|---|
| v1 | 0 | 18 | 17.0% |
| v2.3 | **2** | 6 | **41.5%** (concept) |
| v3.0 | 0 | 11 | 23.0% |

v2.3의 중복 2건은 §4의 결함 F-1에서 원인을 규명했다.

---

## 3. 남은 WARN — T-01 미달 39건의 내역

| 출처 | n | 중앙값 | 800 미만 |
|---|---|---|---|
| v1 유래 | 47 | 658 | **32** |
| v2.3 유래 | 46 | 1074 | 7 |
| 집필 | 7 | — | 0 |

미달 39건 중 32건이 v1 유래다. **이들은 병합하지 않기로 결정했다.**

v1 청크는 하나가 하나의 주제만 담는 원자 단위로 사람이 직접 쓴 것이다
(`rag-prompt-caching-001`, `rag-doctor-command-001` 등). 크기를 채우려고 인접 주제를
합치면 청크 하나가 두 가지를 말하게 되고, 검색 정밀도가 떨어진다.
**800이라는 하한은 목적이 아니라 대리 지표이며, 대리 지표를 맞추려고 목적을 해치지 않는다.**

v2.3 유래 미달 7건은 원본 문서 자체가 짧아 더 합칠 대상이 없는 경우다.

기준을 낮춰 PASS로 만들 수도 있었으나 그렇게 하지 않았다. WARN으로 남기고 사유를
기록하는 편이 다음 사람에게 정직하다.

---

## 4. 원본에서 발견한 결함

### F-1 — v2.3에 내용 없는 채움 청크가 존재 (실제 결함)

`chunk.074-troubleshooting-setup.md` 본문 전체:

```
# 트러블슈팅 가이드 - 설정법

해당 없음 — 트러블슈팅 가이드는 문제 발생 시 참조용입니다.
```

7섹션 고정 템플릿이 **내용이 없는 섹션에도 청크를 강제 생성**한 결과다. 이 청크는
"트러블슈팅 설정법" 류 질의에 매칭되면서 아무 정보도 주지 않는다. 검색 오염이다.

- 영향 범위: 전수 검사 결과 1건
- 조치: v3.0 빌더가 `해당 없음` 패턴 + 120토큰 미만 조건으로 제거
- 교훈: 고정 템플릿은 커버리지를 보장하지만 **빈 칸도 함께 생산**한다

### F-2 — v2.3 트러블슈팅 청크 극단적 크기 편차

같은 문서에서 나온 조각들의 추정 토큰: 95 / 59 / **1623** / 155 / 136 / 113 / 195.
`operations` 하나가 나머지 6개 합의 3배다. 섹션 단위 분할이 크기를 전혀 통제하지 못했다는 증거.

### F-3 — v1 `generate_rag_chunks.py`의 절대경로 하드코딩

```python
OUTPUT_DIR = "/home/user/workspace/rag_chunks"
```
다른 환경에서 실행 불가. v3.0의 `tools/build_v3.py`는 전 경로를 CLI 인자로 받는다.

### F-4 — 두 세트의 스키마 비호환

| | v1 | v2.3 |
|---|---|---|
| ID 형식 | `rag-hooks-001` | `chunk.017` |
| 메타데이터 위치 | frontmatter | frontmatter + 매니페스트 분리 |
| 고유 필드 | audience, use_cases, retrieval_questions, priority, confidence, freshness | section_id, source_file, source_urls |

같은 인덱스에 넣으면 메타데이터 필터가 절반의 청크에서만 동작한다. v3.0에서 단일 스키마로 통일.

### F-5 — 파생 청크와 원본 전문이 같은 트리에 공존

v2.3의 `sources/imported/`에는 원본 리서치 전문(1,483줄 + 544줄)이 들어 있고
`06_rag_chunks/chunks/`는 그 파생물이다. 적재 스크립트를 `**/*.md`로 걸면 **같은 내용이
두 번 색인**된다. v3.0은 `kb/ingestion.yaml`에 include/exclude를 명시했다.

---

## 5. 빌드 과정에서 내가 만든 결함 (자체 발견·수정)

숨기지 않고 기록한다. 상세 경위는 [`LESSONS-LEARNED.md`](LESSONS-LEARNED.md) 참조.

### B-1 — 병합 로직의 aliasing 버그로 청크 2개 유실 (심각)

```python
buckets[-2] += buckets.pop()      # 결함
```
`buckets.pop()`이 먼저 평가돼 리스트가 줄어들면서 `-2`가 한 칸 밀린다. 결과적으로
**다른 버킷을 덮어쓰고** `chunk.073`, `chunk.074`를 통째로 유실시켰다.

- 검출: T-06 중복 시험이 Jaccard **0.986** 쌍을 잡아냄 → 수동 diff로 원인 규명
- 조치: `merge_undersized()`로 재작성, 양수 인덱스 + 새 리스트 반환
- **재발 방지**: 빌더에 보존 불변식 `assert`를 심었다(T-10). 입력 조각 ID 집합과 출력
  버킷의 조각 ID 집합이 정확히 일치하지 않으면 빌드가 실패한다.

이 버그는 조용했다. 출력 청크 수는 정상으로 보였고, 중복 시험이 없었다면 넘어갔을 것이다.

### B-2 — 꼬리 버킷만 병합해 중간에 소형 버킷 잔존

B-1 수정 후 채움 청크(F-1)를 제거하자 앞쪽 버킷이 128토큰으로 홀로 남았다.
꼬리 하나만 처리하던 로직의 한계. 인접 버킷 흡수를 반복하는 방식으로 일반화했다.

### B-3 — 카탈로그 섹션에서 retrieval_questions 미생성 5건

섹션 템플릿에 없는 `reference-servers` 같은 섹션이 질문 없이 통과했다.
T-04가 잡아냈고, 실제 소제목에서 질문을 만드는 폴백을 추가했다.

---

## 6. v3.0 구성

| 출처 | 청크 수 | confidence |
|---|---|---|
| v1 유래 | 47 | verified 42 / mixed 5 |
| v2.3 유래 | 46 | auto-merged 46 |
| 신규 집필 | 7 | draft 7 |
| **합계** | **100** | |

원본 182청크(47+135) → 100청크. 감소는 손실이 아니라 **과분할 복구**다.
추정 총 토큰은 32,512+52,972 = 85,484 → 94,985로 **증가**했다(신규 집필분 포함).

### 신규 집필 7종 (프롬프트 B 갭 분석 결과)

| chunk_id | 주제 | 우선순위 |
|---|---|---|
| `v3-onboarding-day1` | 신규 사용자 첫날 경로 | high |
| `v3-mcp-version-migration` | MCP 사양 마이그레이션 절차 | high |
| `v3-budget-management` | 비용 예산 책정·한도 관리 | high |
| `v3-incident-runbook` | 장애 대응 런북 | high |
| `v3-compliance-audit` | 보안 감사·컴플라이언스 대응 | medium |
| `v3-rag-pipeline-design` | RAG 파이프라인 설계 (프롬프트 C 대응) | high |
| `v3-korean-rag` | 한국어 임베딩·토큰 예산 | high |

전부 `confidence: draft`다. 원본 문서에 근거가 있는 부분과 설계 판단인 부분을
각 청크 본문에 "집필 근거" 블록으로 구분해 두었다.

---

## 7. 품질 게이트 실행 결과

```
$ ./tests/run_checks.sh
=== v3.0 품질 게이트 ===
  PASS  T-01~T-08 청크 감사
  PASS  매니페스트 ↔ 청크 파일 수 일치
  PASS  ingestion exclude 경로 실재 확인
  PASS  골든셋 생성
  PASS  tools/*.py 컴파일

결과: 통과 — 적재 가능
```

### 골든셋
```
청크 100개 → 골든 쌍 408개
청크당 평균 질문 4.08개
  출처 v2.3: 218개
  출처 v1: 160개
  출처 authored: 30개
```

(수치는 §9의 3단계 질문 개선 반영 후 기준.)

---

## 8. 실행하지 못한 시험 — 정직한 공백

| 시험 | 상태 | 이유 |
|---|---|---|
| T-11 내용 사실성 | **부분 실행** | §9 참조 — 조기 만료 항목만 공식 문서 대조. 나머지 미검증 |
| T-12 검색 품질 (Recall/MRR) | **부분 실행** | §9 참조 — BM25 하한만. dense/hybrid 미실행 |
| 임베딩 모델 비교 | **미실행** | `bge-m3` 권장은 코퍼스 성격·폐쇄망 제약에서 도출한 판단이지 측정 결과가 아니다 |
| 실물 토크나이저 검증 | **불가** | 프록시가 BPE 파일 호스트를 403 차단. 전체 토큰 수치는 추정 |

(아래는 §9 실행 전 시점의 기록이며, 부분 실행 결과는 §9에 있다.)

**T-11과 T-12를 완전히 통과하기 전까지 이 지식 베이스를 "검증됐다"고 말해서는 안 된다.**
현재 통과한 것은 구조적 품질뿐이다.

### 프롬프트 D(개별 청크 내용 심층 검토)에 대하여
요청된 프롬프트 D는 무작위 5개 청크의 사실 정확성·최신성·출처 표기를 검토하는 항목이다.
이번 작업에서는 **수행하지 않았다.** 구조 시험(T-01~T-10)은 계산으로 판정 가능하지만
프롬프트 D는 외부 공식 문서 대조가 필요하고, 이 환경에서는 다수 도메인 접근이 프록시에
막혀 있어 "확인했다"고 말할 수 있는 수준의 검증이 불가능했다.
근거 없이 통과 판정을 내리느니 미실행으로 남긴다.

---

## 9. 사후 검증 실행 결과 (2026-08-05, 4단계 진행)

시험 계획 §5의 순서대로 조기 만료 → draft → auto-merged → 검색평가를 실행했다.

### 1단계 — 조기 만료 항목 (공식 문서 대조)

| 검증 대상 | 결과 |
|---|---|
| MCP 2026-07-28 리비전 상태 | **오류 발견 → 정정** |
| Opus 5 / Sonnet 5 가격 배수 | 정확 |
| Claude 4.7+ 토크나이저 30% 증가 | 정확 |
| prompt caching 배수 (1.25x/2x/0.1x), Batch 50% | 정확 |
| 5분 캐시 손익분기 ~1.4회 | 산술적으로 정확 (공식 문서 표현보다 정밀) |

**발견한 오류**: 원본 리서치(2026-08-04 작성)는 2026-07-28을 release candidate로
기술했으나, 실제로는 **2026-07-28에 정식 stable로 게시**됐다(RC 잠금 2026-05-29).
1차 출처: `github.com/modelcontextprotocol/modelcontextprotocol/releases` —
"This release marks the stable release of the `2026-07-28` revision".

조치: `kb/corrections.yaml`에 정정 C-001 등록 → 빌드 시 3개 청크에 자동 적용.
`v3-mcp-version-migration`은 권고 자체가 무효화되어(“정식화까지 보류”) 본문을 다시 썼다.
검증했으나 맞았던 항목도 `verified_no_change`에 V-001~V-004로 기록했다 —
"확인했는데 맞았다"와 "확인 안 했다"는 다른 상태다.

### 2단계 — `confidence: draft` 7건
사내 정보(포털 주소, 온콜 경로, 예산 실측치, 법무 판단)는 **채우지 않았다.**
추측해서 적으면 그럴듯한 오답이 되고 RAG 사용자가 그대로 따른다.
필요한 항목을 [`INTERNAL-FILL-INS.md`](INTERNAL-FILL-INS.md)에 담당·우선순위와 함께 정리했다.

### 3단계 — `auto-merged` 46건 질문 개선
템플릿 전용 생성("X 운영 시 주의점은?")을 **본문 소제목 기반**으로 교체했다.
실측 신호: H3/H4 소제목 45/46 청크, 굵은 용어 46/46 청크 보유.

| | 개선 전 | 개선 후 |
|---|---|---|
| 청크당 질문 | 3.57 | **4.08** |
| v2.3 유래 질문 수 | 168 | **218** |
| 한글 조사 오류 | (미측정) | **0** |

조사 오류는 종성 판별(`josa()`)로 제거했다 — "워크플로우은" 같은 질문은 실제 질의와
겹치지 않는다.

### 4단계 — BM25 검색 평가 (T-12의 실행 가능한 절반)

dense 검색은 벡터 DB가 없어 못 돌리지만 BM25는 순수 파이썬으로 가능하다.
이 코퍼스는 영어 식별자 비중이 높아 어휘 검색이 하이브리드의 실제 한 축이다.

| 색인 대상 | Recall@1 | Recall@10 | MRR | 실패율 |
|---|---|---|---|---|
| 제목+태그+질문+본문 | 0.983 | 1.000 | 0.991 | 0.0% |
| 제목+태그+질문 | 0.983 | 1.000 | 0.991 | 0.0% |
| **본문만** | **0.505** | **0.944** | **0.668** | **5.6%** |

**앞의 두 줄은 아무것도 측정하지 않는다.** `retrieval_questions`로 골든셋을 만들고
그 질문을 색인에 넣었으므로 정답을 색인해 두고 그걸로 찾은 것이다.
Recall@10 = 1.000은 성능이 아니라 **누출**이다. 시험 계획이 경고한 낙관 편향이
예상보다 훨씬 컸다(사실상 측정 불가 수준).

**본문 전용 모드가 유일하게 유효한 수치**다. Recall@10 0.944는 하이브리드의 하한이다.

#### 발견 — 다주제 어트랙터
본문 전용 색인에서 오답 1위를 집계한 결과:

| 청크 | 오답 1위 횟수 | 토큰 |
|---|---|---|
| `v3-faq` | **53** | 1,052 |
| `v3-08-context-engineering-01` | 11 | 1,202 |
| `v3-05-claude-md-memory-02` | 10 | 992 |

`v3-faq`는 크기가 정상 범위인데도 어트랙터로 작동한다. 원인은 **다주제**다 —
FAQ는 여러 주제를 얕게 다뤄 많은 질의에 부분 매칭되고, 그 주제를 깊이 다루는 청크의
top-1을 뺏는다.

#### 실패 23건의 성격 — 실제 오답률은 더 낮다
실패 사례를 열어보니 상당수가 **오답이 아니다.**

| 질의 | 라벨 정답 | 검색 1위 |
|---|---|---|
| Claude Code에 MCP 서버를 어떻게 등록하는가? | `v3-cli-registration` | `v3-03-mcp-setup` (이것도 옳음) |
| MCP와 Skills 중 무엇을 먼저 만들어야 하는가? | `v3-component-selection` | `v3-faq` (해당 Q가 실제로 있음) |

**골든셋의 단일 정답 라벨 편향**이다. 5.6%의 실패율은 실제 오답률을 과대평가한다.
다음 반복에서 골든셋을 복수 정답 허용으로 바꿔야 한다.

---

## 10. 스키마 슬롯·T-13·업로드 어댑터 (2026-08-05 추가)

이미지 포함 소스가 나중에 들어올 것에 대비해 슬롯을 미리 열었다. **지금 넣는 비용은
0이고, 나중에 필드를 추가하면 100청크 재빌드·재임베딩이 필요하다.**

### 스키마 슬롯
`source_anchor`(원문 좌표)와 `assets`(이미지)를 정본 스키마에 추가했다(`kb/schema.md`).
중첩 구조라 인라인 JSON으로 싣는다 — YAML flow 스타일이 곧 JSON이라 최소 파서를
늘리지 않고 정확히 읽힌다. 현재 100청크 전부 빈 값이며 매니페스트에는 안정적 키로
항상 실린다(다운스트림 코드가 `KeyError`를 안 만나게).

설계 원칙: **검색은 텍스트로, 제시는 원문으로.** 검색은 청크의 caption/description
텍스트로 하고, 답변 시점에 `source_anchor`로 원본 페이지·이미지를 불러온다.

### T-13 — 그리고 그 자기시험
검사 항목: 깨진 에셋 경로, `caption`/`description_by` 누락, `screened != true`,
`source_anchor.file` 부재·절대경로, 고아 에셋.

선언된 에셋이 0건이라 T-13은 그냥 PASS한다. **한 번도 발화한 적 없는 검사는 작동을
보장하지 않으므로**, 4종 결함을 일부러 담은 픽스처(`tests/fixtures/t13_broken/`)를
만들고 감사가 FAIL을 내는지 게이트에서 확인한다.

```
$ python3 tools/kb_audit.py tests/fixtures/t13_broken/chunks --repo-root .
  T-13 FAIL  에셋·원문 앵커 무결성
    broken_paths     1  (kb/assets/없는파일.png — 파일 없음)
    missing_metadata 1  (caption 누락)
    unscreened       1
    bad_source_anchor 1  (/etc/passwd — 절대경로)
```

빌더에도 게이트를 넣었다 — `screened != true`인 에셋이 있으면 **빌드 자체가 중단**된다.
사내 문서 이미지에는 콘솔 캡처 형태로 토큰·개인정보가 실제로 자주 들어 있다.

### 업로드 어댑터
v2.3 스크립트 6종은 형식이 달라(JSON 배열 vs JSONL, 평면 vs 중첩) 그대로는 못 쓴다.
더 중요한 건 **벡터 DB마다 메타데이터 타입 제약이 다르다**는 점이다.

| 타깃 | 리스트 | 중첩 객체 | 어댑터 처리 |
|---|---|---|---|
| ChromaDB | ✗ | ✗ | 리스트 → 쉼표 결합 |
| Pinecone | ✅ | ✗ | 중첩 → JSON 문자열 |
| Weaviate | ✅ | ✗ | 중첩 → JSON 문자열 |
| Qdrant / FAISS / JSONL | ✅ | ✅ | 그대로 |

평탄화가 no-op이 아님을 확인했다:

```
[chromadb] tags = 'Skills, SKILL.md, progressive-disclosure, frontmatter, agentskills.io'
[qdrant]   tags = ['Skills', 'SKILL.md', 'progressive-disclosure', ...]
```

`--dry-run`은 외부 패키지 없이 돌아 CI에서 6개 타깃 전부 검증한다.
**실제 임베딩 생성과 DB 적재는 클라이언트 패키지가 없어 실행하지 못했다.**

---

## 11. 다음 조치

| 우선순위 | 항목 |
|---|---|
| 1 | 벡터 DB **실적재** 후 dense/hybrid T-12 실행 (어댑터 준비됨, dry-run만 검증) |
| 2 | 실제 사용자 질의 30건 수집 → held-out 세트 구성 |
| 3 | 골든셋을 복수 정답 허용으로 전환 (단일 라벨 편향 제거) |
| 4 | FAQ·체크리스트를 질문 단위 청크로 분할 (어트랙터 완화) |
| 4 | `confidence: draft` 7건의 사내 실정 반영(포털 주소, 온콜 경로, 예산 실측치) |
| 5 | 2026-09-01까지 모델 가격 항목 재확인 |
| 6 | 네트워크 가용 환경에서 실물 토크나이저로 T-01 재측정 |
